spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/riskdb
    username: riskuser
    password: riskpass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
